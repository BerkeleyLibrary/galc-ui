type Closure = {
  id?: string
  startDate?: string,
  endDate?: string,
  note?: string,
  createdAt?: string,
  updatdAt?: string,
  current?: boolean,
  past?: boolean,
  future?: boolean
}

export type { Closure }
